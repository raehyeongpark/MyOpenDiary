<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.bicos.myopendiary.diary.detail.DetailDiaryCommentViewModel" />

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin">

        <TextView
            android:id="@+id/tv_msg"
            style="@style/Base.TextAppearance.AppCompat.Body1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignWithParentIfMissing="true"
            android:background="@{viewModel.isCommentWrittenByMe ? @drawable/bg_my_comment : @drawable/bg_comment}"
            android:padding="@dimen/activity_vertical_margin"
            android:text="@{viewModel.commentMsg}"
            app:setLayoutGravity="@{viewModel.isCommentWrittenByMe}" />

    </RelativeLayout>

</layout>