<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="viewModel"
            type="com.bicos.myopendiary.diary.write.WriteDiaryViewModel" />

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v7.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/btn_write"
            android:layout_margin="@dimen/activity_horizontal_margin"
            android:transitionName="diary_container">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/activity_horizontal_margin">

                <EditText
                    android:id="@+id/tv_title"
                    style="@style/Base.TextAppearance.AppCompat.Title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/activity_vertical_margin"
                    android:hint="@string/hint_input_title"
                    android:onTextChanged="@{(title, a,b,c) -> viewModel.inputTitle(title)}" />

                <CheckBox
                    android:layout_below="@+id/tv_title"
                    android:layout_marginBottom="@dimen/activity_vertical_margin"
                    android:id="@+id/cb_public"
                    android:text="@string/checkbox_is_public_post"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:onCheckedChanged="@{(checkbox, checked) -> viewModel.changeCheckBox(checked)}"/>

                <EditText
                    android:id="@+id/tv_desc"
                    style="@style/Base.TextAppearance.AppCompat.Body1"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_below="@+id/cb_public"
                    android:hint="@string/hint_input_desc"
                    android:inputType="textMultiLine"
                    android:onTextChanged="@{(desc, a,b,c) -> viewModel.inputDesc(desc)}" />

            </RelativeLayout>

        </android.support.v7.widget.CardView>

        <Button
            android:id="@+id/btn_write"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:onClick="@{(view) -> viewModel.clickWriteDiary()}"
            android:text="write diary" />

    </RelativeLayout>
</layout>


